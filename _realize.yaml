settings:
  legacy:
    force: false
    interval: 100ms
server:
  status: false
  open: false
  port: 5002
  host: localhost
schema:
- name: webrtc-test
  path: .
  commands:
    run:
      status: true
  watcher:
    paths:
    - /
    extensions:
    - go
    - tpl
    ignored_paths:
    - .git
    - vendor
